version: 2
sphinx:
  configuration: docs/conf.py
formats: all
python:
  install:
    - requirements: docs/requirements.txt
    - path: .
    - method: pip
      path: .
      extra_requirements:
        - doc
